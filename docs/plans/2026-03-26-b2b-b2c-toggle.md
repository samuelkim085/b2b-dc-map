# B2B / B2C 토글 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers-extended-cc:executing-plans to implement this plan task-by-task.

**Goal:** AppBar에 B2B/B2C 토글을 추가해서 B2C 주별 집계 데이터로 choropleth를 표시하고, B2C 모드에서 쓸 수 없는 FilterPanel 섹션들을 숨긴다.

**Architecture:** `dataMode: 'b2b' | 'b2c'` state를 App.tsx에 추가. 새 훅 `useB2cData`가 B2C CSV를 파싱해 `Record<string, number>`를 반환. ShipmentsMap은 dataMode에 따라 volumes 소스를 분기하고 B2C 전용 레이어를 스킵. FilterPanel은 B2C 모드에서 호환 불가 섹션을 숨김.

**Tech Stack:** React, TypeScript, PapaParse (이미 설치됨)

---

### Task 1: B2C CSV 배치 및 useB2cData 훅 작성

**Files:**
- Copy: `C:\Github\uc_analysis\output\b2c_qty_by_state_2025.csv` → `public/data/b2c_qty_by_state_2025.csv`
- Create: `src/hooks/useB2cData.ts`

**Step 1: CSV 파일 복사**

```bash
cp C:/Github/uc_analysis/output/b2c_qty_by_state_2025.csv public/data/b2c_qty_by_state_2025.csv
```

**Step 2: useB2cData 훅 작성**

`src/hooks/useB2cData.ts`:

```typescript
import { useState, useEffect } from 'react'
import Papa from 'papaparse'

export function useB2cData(): {
  volumes: Record<string, number>
  loading: boolean
  error: string | null
} {
  const [volumes, setVolumes] = useState<Record<string, number>>({})
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState<string | null>(null)

  useEffect(() => {
    const controller = new AbortController()
    fetch('/data/b2c_qty_by_state_2025.csv', { signal: controller.signal })
      .then(r => {
        if (!r.ok) throw new Error(`Failed to load B2C CSV: ${r.status}`)
        return r.text()
      })
      .then(text => {
        const { data } = Papa.parse<{ ship_to_state: string; total_qty: string }>(text, {
          header: true,
          skipEmptyLines: true,
        })
        const result: Record<string, number> = {}
        for (const row of data) {
          const qty = Number(String(row.total_qty).replace(/,/g, ''))
          if (row.ship_to_state && !isNaN(qty)) {
            result[row.ship_to_state] = qty
          }
        }
        setVolumes(result)
        setLoading(false)
      })
      .catch(err => {
        if ((err as Error).name === 'AbortError') return
        setError(String(err))
        setLoading(false)
      })
    return () => controller.abort()
  }, [])

  return { volumes, loading, error }
}
```

**Step 3: 커밋**

```bash
git add public/data/b2c_qty_by_state_2025.csv src/hooks/useB2cData.ts
git commit -m "feat: add B2C CSV data and useB2cData hook"
```

---

### Task 2: App.tsx에 dataMode 상태 추가

**Files:**
- Modify: `src/App.tsx`

**Step 1: dataMode state 및 useB2cData 연결**

`App.tsx`에서 다음을 추가/변경:

```typescript
// 추가 import
import { useB2cData } from "./hooks/useB2cData";

// App() 함수 내 상단에 추가
const [dataMode, setDataMode] = useState<'b2b' | 'b2c'>('b2b');
const { volumes: b2cVolumes } = useB2cData();
```

**Step 2: AppBar에 props 전달**

```tsx
// 기존
<AppBar svgRef={svgRef} selectedOrigin={selectedOrigin} />

// 변경
<AppBar
  svgRef={svgRef}
  selectedOrigin={selectedOrigin}
  dataMode={dataMode}
  onDataModeChange={setDataMode}
/>
```

**Step 3: FilterPanel에 dataMode prop 전달**

```tsx
<FilterPanel
  ...기존 props...
  dataMode={dataMode}
/>
```

**Step 4: ShipmentsMap에 dataMode, b2cVolumes props 전달**

```tsx
<ShipmentsMap
  ...기존 props...
  dataMode={dataMode}
  b2cVolumes={b2cVolumes}
/>
```

**Step 5: 커밋**

```bash
git add src/App.tsx
git commit -m "feat: add dataMode state to App, wire useB2cData"
```

---

### Task 3: AppBar에 B2B/B2C 토글 버튼 추가

**Files:**
- Modify: `src/components/AppBar.tsx`

**Step 1: Props 인터페이스 확장 및 토글 UI 추가**

```tsx
import type React from "react";
import { ExportButton } from "./ExportButton";
import type { Origin } from "../types";

interface Props {
  svgRef: React.RefObject<SVGSVGElement | null>;
  selectedOrigin: Origin;
  dataMode: 'b2b' | 'b2c';
  onDataModeChange: (mode: 'b2b' | 'b2c') => void;
}

export function AppBar({ svgRef, selectedOrigin, dataMode, onDataModeChange }: Props) {
  return (
    <header className="app-bar">
      <span className="app-bar-title">DC MAP</span>
      <div className="app-bar-mode-toggle">
        <button
          type="button"
          className={`mode-btn${dataMode === 'b2b' ? ' active' : ''}`}
          onClick={() => onDataModeChange('b2b')}
        >
          B2B
        </button>
        <button
          type="button"
          className={`mode-btn${dataMode === 'b2c' ? ' active' : ''}`}
          onClick={() => onDataModeChange('b2c')}
        >
          B2C
        </button>
      </div>
      <div className="app-bar-actions">
        <ExportButton svgRef={svgRef} selectedOrigin={selectedOrigin} />
      </div>
    </header>
  );
}
```

**Step 2: 토글 버튼 CSS 추가**

`src/index.css` 또는 AppBar용 CSS에 아래 추가:

```css
.app-bar-mode-toggle {
  display: flex;
  gap: 2px;
  border: 1px solid var(--accent);
  border-radius: 4px;
  overflow: hidden;
}

.mode-btn {
  background: transparent;
  border: none;
  color: var(--text);
  padding: 3px 12px;
  font-size: 0.75rem;
  font-weight: 600;
  cursor: pointer;
  letter-spacing: 0.05em;
  opacity: 0.5;
  transition: opacity 0.15s, background 0.15s;
}

.mode-btn.active {
  background: var(--accent);
  color: var(--bg);
  opacity: 1;
}

.mode-btn:hover:not(.active) {
  opacity: 0.8;
}
```

**Step 3: 커밋**

```bash
git add src/components/AppBar.tsx src/index.css
git commit -m "feat: add B2B/B2C mode toggle to AppBar"
```

---

### Task 4: ShipmentsMap — B2C 모드 분기 처리

**Files:**
- Modify: `src/components/ShipmentsMap.tsx`

**Step 1: Props 인터페이스 확장**

```typescript
interface Props {
  records: DcRecord[];
  filters: FilterState;
  flowSettings: FlowSettings;
  settings: AppSettings;
  svgRef: React.RefObject<SVGSVGElement | null>;
  dataMode: 'b2b' | 'b2c';          // 추가
  b2cVolumes: Record<string, number>; // 추가
}
```

**Step 2: 함수 시그니처에 destructure 추가**

```typescript
export function ShipmentsMap({ records, filters, flowSettings, settings, svgRef, dataMode, b2cVolumes }: Props) {
```

**Step 3: stateVolumes 계산 분기**

기존 (ShipmentsMap.tsx:284):
```typescript
const stateVolumes = useMemo(
  () => (filters.showChoropleth ? buildStateVolumes(choroplethRecords) : {}),
  [choroplethRecords, filters.showChoropleth],
);
```

변경:
```typescript
const stateVolumes = useMemo(() => {
  if (!filters.showChoropleth) return {};
  if (dataMode === 'b2c') return b2cVolumes;
  return buildStateVolumes(choroplethRecords);
}, [choroplethRecords, filters.showChoropleth, dataMode, b2cVolumes]);
```

**Step 4: B2C 모드에서 DC 마커/zip dots/flow/radiusRing 렌더링 스킵**

JSX에서 DC 마커 렌더링 부분을 `{dataMode === 'b2b' && settings.showDcMarkers && ...}` 조건으로 감싼다. 마찬가지로 zip dots, FlowLayer, OriginRadiusRing도 동일하게 처리.

구체적으로:
- DC 마커 렌더 블록: `{dataMode === 'b2b' && settings.showDcMarkers && markerOffsets.map(...)}`
- zip dots 렌더 블록: `{dataMode === 'b2b' && settings.showZipDots && ...}`
- `<FlowLayer .../>`: `{dataMode === 'b2b' && flowSettings.showFlows && <FlowLayer .../>}`
- `<OriginRadiusRing .../>`: `{dataMode === 'b2b' && filters.showOriginRadiusRing && <OriginRadiusRing .../>}`

> **Note:** ShipmentsMap.tsx의 정확한 JSX 라인 위치를 파악한 후 조건을 추가한다. `settings.showDcMarkers`, `settings.showZipDots`, `flowSettings.showFlows`, `filters.showOriginRadiusRing` 조건이 이미 있는 곳에 `dataMode === 'b2b' &&`를 앞에 붙이면 된다.

**Step 5: 커밋**

```bash
git add src/components/ShipmentsMap.tsx
git commit -m "feat: ShipmentsMap branches on dataMode for volumes and layer rendering"
```

---

### Task 5: FilterPanel — B2C 모드에서 섹션 숨기기

**Files:**
- Modify: `src/components/FilterPanel.tsx`

**Step 1: Props 인터페이스에 dataMode 추가**

```typescript
interface Props {
  ...기존 props...
  dataMode: 'b2b' | 'b2c';
}
```

**Step 2: 함수 시그니처 destructure에 추가**

```typescript
export function FilterPanel({
  filters, onChange, flowSettings, onFlowChange,
  settings, onSettingsChange, allCustomers, origins,
  flowDestinationOptions, maxVolume, maxDistance, panelWidth,
  dataMode,  // 추가
}: Props) {
```

**Step 3: B2C에서 숨길 섹션들을 조건부 렌더링으로 변경**

JSX에서 아래 4개 `<PanelSection>` 블록을 `{dataMode === 'b2b' && <PanelSection .../>}` 로 감싼다:
- `sectionKey="dcLocations"` — DC Locations 섹션
- `sectionKey="radiusRing"` — Radius Ring 섹션
- `sectionKey="flowLayer"` — Flow Layer 섹션
- `sectionKey="markerStyle"` — Marker Style 섹션

**Step 4: qtyByState 섹션 내 고객 필터 드롭다운 숨기기**

`qtyByState` 섹션 내 `choroplethCustomers` 관련 `<CustomerDropdown>` 또는 필터 UI를 `{dataMode === 'b2b' && ...}` 로 감싼다.

**Step 5: 커밋**

```bash
git add src/components/FilterPanel.tsx
git commit -m "feat: FilterPanel hides B2B-only sections in B2C mode"
```

---

### Task 6: 동작 검증

**Step 1: 개발 서버 실행**

```bash
npm run dev
# 또는 포트 충돌 시:
npx vite --port 3000 --host
```

**Step 2: 수동 확인 항목**

- [ ] AppBar에 `B2B | B2C` 토글 버튼 표시
- [ ] B2C 클릭 시 choropleth가 B2C 주별 데이터로 갱신됨 (CA, FL, TX 등이 진하게)
- [ ] B2C 모드에서 FilterPanel의 DC Locations, Radius Ring, Flow Layer, Marker Style 섹션 사라짐
- [ ] B2C 모드에서 qtyByState 섹션의 고객 필터 드롭다운 사라짐
- [ ] B2C 모드에서 DC 마커/zip dots/flow 화살표가 지도에서 사라짐
- [ ] B2B 클릭 시 모든 것이 원상 복귀

**Step 3: 빌드 확인**

```bash
npm run build
```

Expected: 타입 에러 없이 빌드 성공.

**Step 4: 유닛 테스트 통과 확인**

```bash
npm run test
```

---
